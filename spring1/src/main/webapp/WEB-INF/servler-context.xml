<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://www.springframework.org/schema/beans"
    xmlns:xsi="https://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="https://www.springframework.org/schema/context"
    xmlns:mvc="https://www.springframework.org/schema/mvc" 
    xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        https://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        https://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">
        
        
        
    <!-- Spring Web MVC 설정들을 애너테이션 기반으로 설정할 수 있도록 -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.StringHttpMessageConverter"> <!-- 기본생성자호출. -->
                <property name="supportedMediaType">
                    <list>
                        <value>text/html; charset=UTF-8</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>
    
    <!-- 스프링mvc에서 dispatcherServlet이 처리하지 않는 정적 요청에 필요한 파일들
    (이미지, css, js,)이 있는 폴더위치를 설정.
    정적리소스(이미지,,css,js,)들을 /webapp/static/폴더 아래에 작성. 
    요청주소가http://server:port/contextPath/static/... 시작하면, 요청을 dispatcherServlet 이 처리하지않고
    해당 폴더 아래의 파일들을 클라이언트로 바로 응답보냄-->
    <mvc:resources location="/static/" mapping="/**" />
    
    
</beans>
